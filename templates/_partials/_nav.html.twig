<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site E-commerce</title>
    
    <!-- Lien vers FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    
    <!-- Autres styles de ton projet -->
    <link rel="stylesheet" href="path/to/your/styles.css">
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <!-- Logo avec icône -->
            <a class="navbar-brand" href="{{ path('app_main') }}">
                <i class="fas fa-home" style="font-size: 30px; color: #DDA0DD;"></i> <!-- Icône Accueil -->
            </a>
            <!-- Bouton de menu pour mobile -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <!-- Menu de navigation -->
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <!-- Icône Panier -->
                        <a class="nav-link active" aria-current="page" href="{{ path('cart_index') }}">
                            <i class="fas fa-shopping-cart" style="font-size: 30px; color:#DDA0DD;"></i> <!-- Icône de chariot -->
                        </a>
                    </li>

                    <!-- Icône Recherche -->
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                        </a>
                    </li>
                </ul>

                <!-- Menu utilisateur -->
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    {% if app.user %}
                        {% if is_granted('ROLE_PRODUCT_ADMIN') %}
                            <li class="nav-item">
                                <a a class="nav-link text-dark fw-bold fs-4" href="{{ path('admin_index') }}">Administration</a>
                            </li>
                        {% endif %}
                       
                        <li class="nav-item">
                            <a class="nav-link text-dark fw-bold fs-4" href="{{ path('app_logout') }}">Log out
                            </a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link text-dark fw-bold fs-4" href="{{ path('app_register') }}">
                                Register</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-dark fw-bold fs-4" href="{{ path('app_login') }}">Log in</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

</body>
</html>
